package Class2;

public class DecimalNumber {
    public static void main(String[] args) {
        /*
        if we want store decimal number in computer's memory using java we can use following float decimal

         */
        float myNumberBox=10.5f;
        float mySecondBox=5.5f;
        double largeDecimalBoc=134545455.12121;
        System.out.println(myNumberBox);
        /*
        if we want to store characters(#,$,o,s,4) we can use char datatype

         */
        char letter='s';
        char gender='M';
        char symbol='#';
        char numberChar='1';
        /*
        string data type belongs to a different category called non-primitives that why for string rules are a little different for exmple S of String is upper case its color is also not blue we will discuss these datails later on.

         */
        String name="Sam";

        boolean hungry=true; // true means yes

    }
}
